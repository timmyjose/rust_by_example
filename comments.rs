fn main() {
    // These are C++-like line comments
    // Each of these comments extends to the end of the current line

    /* This is a block comment
    that can be /* nested */ to arbitrary depths*/
    let x = 5 + /* 90 + */ 5;
    println!("Is 'x' 10 or 100? x = {}", x);
}
